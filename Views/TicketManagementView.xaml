<UserControl x:Class="CinemaSystem.Desktop.Views.TicketManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#121212">
    <UserControl.Resources>
        <Style x:Key="CustomCalendarDayButtonStyle" TargetType="CalendarDayButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CalendarDayButton">
                        <Border x:Name="DayBorder" Background="{TemplateBinding Background}" CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="DayBorder" Property="Background" Value="#444"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="DayBorder" Property="Background" Value="#FFD700"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                            <Trigger Property="IsToday" Value="True">
                                <Setter TargetName="DayBorder" Property="BorderBrush" Value="#FFD700"/>
                                <Setter TargetName="DayBorder" Property="BorderThickness" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsInactive" Value="True">
                                <Setter Property="Foreground" Value="#555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CalendarNavButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="25"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="Background" Value="#FFD700"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CustomCalendarItemStyle" TargetType="{x:Type CalendarItem}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type CalendarItem}">
                        <ControlTemplate.Resources>
                            <DataTemplate x:Key="{x:Static CalendarItem.DayTitleTemplateResourceKey}">
                                <TextBlock Foreground="#888" FontWeight="Bold" FontSize="10" HorizontalAlignment="Center" Margin="0,5" Text="{Binding}"/>
                            </DataTemplate>
                        </ControlTemplate.Resources>
                        <Grid x:Name="PART_Root">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0" Background="Transparent" Margin="0,5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Button x:Name="PART_PreviousButton" Grid.Column="0" Style="{StaticResource CalendarNavButtonStyle}">
                                        <Path Data="M 6,0 L 0,6 L 6,12" Stroke="Black" StrokeThickness="2" Margin="3,0,0,0"/>
                                    </Button>
                                    <Button x:Name="PART_HeaderButton" Grid.Column="1" Foreground="#FFD700" FontWeight="Bold" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center" Background="Transparent" BorderThickness="0" IsHitTestVisible="False"/>
                                    <Button x:Name="PART_NextButton" Grid.Column="2" Style="{StaticResource CalendarNavButtonStyle}">
                                        <Path Data="M 0,0 L 6,6 L 0,12" Stroke="Black" StrokeThickness="2" Margin="0,0,3,0"/>
                                    </Button>
                                </Grid>
                            </Border>
                            <Grid Grid.Row="1">
                                <Grid x:Name="PART_MonthView">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                </Grid>
                                <Grid x:Name="PART_YearView" Visibility="Hidden">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                </Grid>
                            </Grid>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CustomCalendarStyle" TargetType="Calendar">
            <Setter Property="Background" Value="#1F1E1E"/>
            <Setter Property="BorderBrush" Value="#FFD700"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="CalendarDayButtonStyle" Value="{StaticResource CustomCalendarDayButtonStyle}"/>
            <Setter Property="CalendarItemStyle" Value="{StaticResource CustomCalendarItemStyle}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Calendar">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" Padding="10">
                            <StackPanel x:Name="PART_Root" HorizontalAlignment="Center">
                                <CalendarItem x:Name="PART_CalendarItem" Style="{TemplateBinding CalendarItemStyle}"/>
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CinemaDatePickerStyle" TargetType="DatePicker">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#333"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="CalendarStyle" Value="{StaticResource CustomCalendarStyle}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePicker">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="5" Padding="5">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="30"/>
                                </Grid.ColumnDefinitions>
                                <DatePickerTextBox x:Name="PART_TextBox" Grid.Column="0" Foreground="White" Background="Transparent" SelectionBrush="#FFD700" VerticalAlignment="Center" Margin="5,0,0,0" BorderThickness="0" FontSize="14">
                                    <DatePickerTextBox.Style>
                                        <Style TargetType="DatePickerTextBox">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="DatePickerTextBox">
                                                        <ScrollViewer x:Name="PART_ContentHost"/>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </DatePickerTextBox.Style>
                                </DatePickerTextBox>
                                <Button x:Name="PART_Button" Grid.Column="1" Background="Transparent" BorderThickness="0" Cursor="Hand">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="Transparent">
                                                <Path Data="M19,4H18V2H16V4H8V2H6V4H5C3.89,4 3,4.9 3,6V20A2,2 0 0,0 5,22H19A2,2 0 0,0 21,20V6A2,2 0 0,0 19,4M19,20H5V10H19V20M19,8H5V6H19V8M9,14H7V12H9V14M13,14H11V12H13V14M17,14H15V12H17V14M9,18H7V16H9V18M13,18H11V16H13V18M17,18H15V16H17V18Z" Fill="#FFD700" Width="18" Height="18" Stretch="Uniform"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                                <Popup x:Name="PART_Popup" StaysOpen="False" AllowsTransparency="True" Placement="Bottom">
                                    <Calendar x:Name="PART_Calendar" Style="{StaticResource CustomCalendarStyle}"/>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter Property="BorderBrush" Value="#FFD700"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#1E1E1E" Height="100" Padding="20" BorderBrush="#333" BorderThickness="0,0,0,1">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="*"/>    <ColumnDefinition Width="Auto"/> </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" 
                   Text="ZARZĄDZANIE BILETAMI" 
                   Foreground="#FFD700" FontSize="24" FontWeight="Bold" 
                   VerticalAlignment="Center"/>

        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">

            <Border Background="#333" CornerRadius="5" Width="300" Height="40" Margin="0,0,20,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="🔍" Foreground="Gray" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                             Background="Transparent" BorderThickness="0" Foreground="White" 
                             CaretBrush="#FFD700" VerticalAlignment="Center" FontSize="14" Margin="5,0,5,0"
                             Tag="Szukaj biletu..."/>
                </Grid>
            </Border>

            <TextBlock Text="DATA:" Foreground="Gray" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="14" FontWeight="SemiBold"/>

            <DatePicker SelectedDate="{Binding SelectedDate}" 
                        Width="140" Height="40"
                        VerticalAlignment="Center" VerticalContentAlignment="Center"
                        Style="{StaticResource CinemaDatePickerStyle}"/>

            <Button Command="{Binding LoadTicketsCommand}" Content="ODŚWIEŻ" 
                    Margin="15,0,0,0" Padding="15,0" Height="40"
                    Background="#333" Foreground="White" FontWeight="Bold" BorderThickness="0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>

        </StackPanel>
    </Grid>
</Border>

        <DataGrid Grid.Row="1" ItemsSource="{Binding FilteredTickets}" AutoGenerateColumns="False"
                  CanUserAddRows="False" IsReadOnly="True"
                  Background="Transparent" RowBackground="#1A1A1A" AlternatingRowBackground="#222"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                  BorderThickness="0" Foreground="White" RowHeight="50">
            
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#888"/>
                    <Setter Property="Padding" Value="10"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="BorderBrush" Value="#444"/>
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="10"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Padding="{TemplateBinding Padding}" BorderBrush="Transparent" BorderThickness="0" Background="Transparent">
                                    <ContentPresenter VerticalAlignment="Center" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#333"/>
                            <Setter Property="Foreground" Value="#FFD700"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Header="#" Binding="{Binding Id}" Width="50"/>
                
                <DataGridTextColumn Header="TRANSAKCJA" Binding="{Binding TransactionNumber}" Width="120"/>

                <DataGridTemplateColumn Header="KLIENT" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Reservation.CustomerName}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Reservation.CustomerEmail}" FontSize="11" Foreground="LightGray"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="SEANS" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Reservation.Screening.Movie.Title}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Reservation.Screening.Start, StringFormat=dd.MM.yyyy}" FontSize="11" Foreground="LightGray" Margin="0,0,10,0"/>
                                    <TextBlock Text="{Binding Reservation.Screening.Start, StringFormat=HH:mm}" FontSize="11" Foreground="#FFD700" FontWeight="Bold"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="MIEJSCE" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <Run Text="Rz:" Foreground="LightGray"/><Run Text="{Binding Row}" FontWeight="Bold"/>
                                <Run Text=" M:" Foreground="LightGray"/><Run Text="{Binding SeatNumber}" FontWeight="Bold"/>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="AKCJE" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Command="{Binding DataContext.StartEditCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                        CommandParameter="{Binding}"
                                        ToolTip="Edytuj dane klienta" Background="Transparent" BorderThickness="0" Margin="0,0,5,0">
                                    <TextBlock Text="✏️"/>
                                </Button>
                                
                                <Button Command="{Binding DataContext.ResendTicketCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                        CommandParameter="{Binding}"
                                        ToolTip="Wyślij bilet ponownie" Background="Transparent" BorderThickness="0" Margin="0,0,5,0">
                                    <TextBlock Text="📧"/>
                                </Button>

                                <Button Command="{Binding DataContext.ChangeSeatCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                        CommandParameter="{Binding}"
                                        ToolTip="Zmień miejsce" Background="Transparent" BorderThickness="0" Margin="0,0,5,0">
                                    <TextBlock Text="💺"/>
                                </Button>

                                <Button Command="{Binding DataContext.RefundSingleTicketCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                        CommandParameter="{Binding}"
                                        ToolTip="Zwróć TEN bilet" Background="Transparent" BorderThickness="0" Margin="0,0,5,0">
                                    <TextBlock Text="❌" Foreground="#FF5555"/>
                                </Button>

                                <Button Command="{Binding DataContext.RefundFullReservationCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                        CommandParameter="{Binding}"
                                        ToolTip="Zwróć całą transakcję" 
                                        Background="#990000" Foreground="White" 
                                        Padding="5,0" Margin="5,0,0,0" 
                                        FontSize="10" FontWeight="Bold" BorderThickness="0">
                                    <TextBlock Text="ZWROT TRANS."/>
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="3"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.RowSpan="2" Background="#AA000000" 
              Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Background="#252525" Width="400" VerticalAlignment="Center" CornerRadius="10" Padding="20" BorderBrush="#FFD700" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="EDYCJA DANYCH KLIENTA" FontSize="18" FontWeight="Bold" Foreground="#FFD700" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                    
                    <TextBlock Text="Imię i Nazwisko:" Foreground="LightGray" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding EditCustomerName}" Padding="5" Background="#333" Foreground="White" BorderThickness="0"/>
                    
                    <TextBlock Text="Adres E-mail:" Foreground="LightGray" Margin="0,15,0,5"/>
                    <TextBox Text="{Binding EditCustomerEmail}" Padding="5" Background="#333" Foreground="White" BorderThickness="0"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                        <Button Content="Anuluj" Command="{Binding CancelEditCommand}" Width="100" Margin="0,0,10,0" Padding="5"
                                Background="#E50914" Foreground="White" FontWeight="Bold" BorderThickness="0">
                             <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="3"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                        
                        <Button Content="Zapisz" Command="{Binding SaveEditCommand}" Width="100" Background="#FFD700" Foreground="Black" FontWeight="Bold" Padding="5" BorderThickness="0">
                             <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="3"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>