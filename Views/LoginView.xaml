<UserControl x:Class="CinemaSystem.Desktop.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:CinemaSystem.Desktop.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             
             FocusManager.FocusedElement="{Binding ElementName=LoginField}"> <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#0F0F0F" Offset="0.0"/>
                <GradientStop Color="#2A2A2A" Offset="1.0"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Border Background="#1E1E1E" Width="400" Height="450" CornerRadius="10">
            <Border.Effect>
                <DropShadowEffect Color="Black" Direction="270" ShadowDepth="10" Opacity="0.5" BlurRadius="20"/>
            </Border.Effect>
            
            <StackPanel Margin="40" VerticalAlignment="Center">
                
                <TextBlock Text="KINO SYSTEM" Foreground="#FFD700" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="#FFD700" ShadowDepth="0" BlurRadius="10" Opacity="0.3"/>
                    </TextBlock.Effect>
                </TextBlock>

                <TextBlock Text="LOGIN" Foreground="Gray" FontSize="12" Margin="0,0,0,5"/>
                
                <TextBox x:Name="LoginField" 
                         Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                         Background="#333" 
                         Foreground="White"
                         BorderThickness="0"
                         Padding="10"
                         FontSize="14"/>

                <TextBlock Text="HASŁO" Foreground="Gray" FontSize="12" Margin="0,20,0,5"/>
                <PasswordBox x:Name="UserPasswordBox"
                             Background="#333" 
                             Foreground="White"
                             BorderThickness="0"
                             Padding="10"
                             FontSize="14"/>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="#FF4444" FontSize="12" Margin="0,15,0,0" HorizontalAlignment="Center" TextWrapping="Wrap"/>

                <Button Content="ZALOGUJ SIĘ"
                        Command="{Binding LoginCommand}"
                        CommandParameter="{Binding ElementName=UserPasswordBox}"
                        IsDefault="True"
                        Background="#FFD700"
                        Foreground="Black"
                        FontWeight="Bold"
                        Padding="10"
                        Margin="0,30,0,0"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>